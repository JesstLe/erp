# ERP 管理端前端

React 19、TypeScript、Vite 和 Ant Design 管理端。开发服务器只绑定 `127.0.0.1`，并把 `/api` 和 `/health` 代理到本地 API。

常用命令：

- `npm run dev`：启动本地开发服务器。
- `npm run lint`：运行 Oxlint。
- `npm test`：运行 Vitest 窄测试。
- `npm run build`：类型检查并生成生产资源。
