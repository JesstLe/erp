# ERP 开发进度

更新日期：2026-08-18

| 模块 | 状态 | 代码 | 窄测试 | 使用说明 | 截图 |
|---|---|---|---|---|---|
| 工程与本地运行基线 | 基线完成 | .NET/React/PostgreSQL、迁移、配置与安全中间件 | 编译、依赖审计、迁移烟测通过 | [本地启动与登录](user-manual/01-local-login-and-permissions.md) | 不适用 |
| 身份、组织与权限 | 首个闭环完成 | 登录/退出、租户门店、员工档案、账号、角色/门店分配、首次改密、账号启停 | 3 个员工领域测试；API/浏览器账号生命周期与职责分离烟测 | [员工与权限](user-manual/09-employees-accounts-and-permissions.md) | 3 张 |
| 目录与价格 | 首个闭环完成 | 服务项目/产品新建与列表、库存属性预留、综合价格草稿/发布与历史快照 | 9 个目录领域测试；空库迁移/API/浏览器烟测 | [产品目录与标准价](user-manual/10-product-catalog-and-pricing.md) | 6 张 |
| 设施接待与计时 | 首个闭环完成 | 配置/看板、开始、暂停、继续、换设施、结束、清洁 | 4 个新增领域测试；迁移/API/浏览器闭环烟测 | [设施接待与计时](user-manual/03-facility-reception-and-timing.md) | 5 张 |
| 顾客与会员 | 首个闭环完成 | 脱敏查询、加密建档、卡类、开卡、三类账户、不可变流水基线 | 4 个新增领域测试；迁移/API/浏览器闭环烟测 | [顾客与会员](user-manual/04-customers-and-membership.md) | 5 张 |
| 服务录单与金额确认 | 首个闭环完成 | 待录单接待、消费单草稿、价格快照、改价原因、金额确认 | 5 个新增领域测试；空库迁移/API 幂等与并发/浏览器闭环烟测 | [服务录单与金额确认](user-manual/05-service-order-and-price-confirmation.md) | 3 张 |
| 现金、人工外部收款与交班 | 首个闭环完成 | 可配置支付方式、组合分摊、现金/人工外部登记、结算、开班/交班快照、独立复核队列 | 8 个新增领域测试；空库迁移/API 幂等和状态/浏览器闭环烟测 | [支付与收银交班](user-manual/06-payments-and-cashier-shifts.md) | 7 张 |
| 会员储值与资金分账 | 首个闭环完成 | 储值单、通用支付来源、本金/奖励金分账、不可变流水、交班汇总、收入隔离 | 3 个新增领域测试；迁移/数据库/浏览器闭环烟测 | [会员储值与资金分账](user-manual/11-member-topups.md) | 2 张 |
| 会员余额扣款与验证码 | 本地闭环完成 | 内部账户支付、完整手机号核对、500 元一次性验证码、原子扣款、不可变借方流水 | 3 个新增领域测试；迁移/数据库/浏览器闭环烟测 | [会员余额消费与手机号验证](user-manual/12-member-balance-payments.md) | 3 张 |
| 真实渠道与退款 | 待开发 | — | — | [V2 范围](v2-scope-and-delivery-plan.md) | — |
| 审计记录 | 首个闭环完成 | 门店范围、动作/对象/日期筛选、服务端分页、只读详情 | API 范围/过滤/非法日期烟测；浏览器列表与详情验收 | [审计记录](user-manual/07-audit-events.md) | 2 张 |
| 经营工作台与报表 | 首个闭环完成 | 今日实时指标、7–92 日趋势、支付构成、项目排行、设施记录时长 | 1 个新增时段裁剪测试；API 口径/日期上限烟测；浏览器验收 | [经营工作台与报表](user-manual/08-dashboard-and-operations-reports.md) | 3 张 |

状态只以可运行代码和对应测试证据为准，设计文档或静态界面不单独视为模块完成。

## 本轮证据

- API 编译：成功，0 警告、0 错误。
- 当前全量后端测试：领域单元测试 40/40、仓库集成测试 5/5，通过且无跳过。
- 前端安全请求测试：2/2 通过。
- 前端静态检查：通过，0 警告。
- 前端生产构建：通过；当前入口包约 1.32 MiB（gzip 约 420 KiB），构建仍提示大包分块建议，代码分包留到页面规模增长后处理。
- 依赖审计：npm 与 NuGet 当前均未发现已知漏洞。
- 数据库迁移：九个前向迁移均从空库在 PostgreSQL 18.3 临时库执行成功，共 40 张表；18.4 目标镜像仍需复验。
- 最终 API 烟测：健康检查、登录、产品、价格版本、员工、已关闭班次和经营报表均为 200；报表保持业务已结算 100 元、外部待核对 60 元。
- 首次改密后端约束：未完成改密账号调用业务接口实测返回 `403 PASSWORD_CHANGE_REQUIRED`，不能通过绕过前端路由访问业务。
- 浏览器：41 张本地真实页面截图覆盖登录、目录价格、设施、顾客会员、储值、会员扣款、收银交班、审计、报表和员工权限；各模块手册图片存在性由集成测试检查。
- V2 会员储值：本地虚构会员现金储值本金 200 元、赠送奖励金 20 元，页面回显与数据库余额一致；产生 2 条不可变贷方流水。
- V2 账务隔离：数据库实测消费支付 100 元与储值收款 200 元按业务来源分开；经营报表代码只汇总消费，储值现金仍归入当前收银班次。
- V2 会员扣款：本地虚构会员先储值本金 700 元，再使用本金支付 600 元消费单；账户最终余额 100 元，支付分摊为 `InternalAccount + InternalConfirmed + NotRequired`。
- V2 扣款流水：同一会员本金账户保留 700 元贷方和 600 元借方两条不可变流水，分别关联储值单和消费单，没有覆盖原入账记录。
- V2 验证码：600 元扣款挑战绑定消费单、顾客和金额，5 分钟有效并在结算事务中推进为 `Used`；前端同时校验已验证金额没有被后续编辑改变。
- 设施闭环：同请求幂等重放命中同一记录；`IN_USE → PAUSED → IN_USE → 换设施 → CLEANING_REQUIRED → AVAILABLE` 真实 API 与浏览器流程通过，刷新后暂停状态保持。
- 设施并发：两个终端同时开始同一设施时实测为 `200 + 409 (VERSION_CONFLICT)`，数据库活动会话保持唯一，验收清理后为 0。
- 设施审计：开始、暂停、继续、更换、结束和清洁完成均已产生审计记录。
- 顾客会员 API：顾客建档、卡类创建和开卡同请求重放均为 200；重复手机号为 409、重复卡类编号为 422；开卡后准确产生 3 个独立零余额账户。
- 会员流水约束：真实 PostgreSQL 中更新和删除均被 `55000` 拒绝，测试事务回滚后未残留流水；从空库连续执行三个迁移得到 28 张表。
- 顾客会员浏览器：完成脱敏列表、新建顾客、卡类表单、开卡和账户详情验收；生效日按门店时区显示为 2026-08-18。
- 隐私配置：非开发环境缺少、过短或仍为占位值的手机号查询密钥时，API 已实测启动失败并给出明确配置错误。
- 消费单 API：直接补录创建与确认的同请求重放均为 `200 + 200` 且命中同一记录；消费单日期按门店时区生成。
- 消费单并发：两个终端同时为同一已结束接待录单，实测为 `200 + 409 (VERSION_CONFLICT)`，有效消费单保持唯一。
- 收银浏览器：消费单列表、新建表单、标准价/成交价差异、改价原因和待支付详情在 1280×720 视口完成真实页面验收。
- 支付结算 API：未开班使用现金/人工外部支付为 `409 SHIFT_NOT_OPEN`；开班和结算同请求重放均为 `200 + 200` 且命中同一记录。
- 组合支付：真实 API 将 100 元应收拆为 40 元现金和 60 元微信人工登记；消费单为已结算，分摊分别为 `CashRecorded + NotRequired` 与 `ManualPendingReconciliation + Pending`。
- 收银交班：理论现金 90 元、实交 90 元、差额 0、外部待核对 60 元形成不可变提交快照；重复提交命中同一班次；原收银员自我复核被 `403 FORBIDDEN_ACTION` 拒绝。
- 支付/交班浏览器：当班概览、组合支付、结算分摊、交班表单和待复核状态在 1280×720 视口完成真实页面验收。
- 审计 API：当前门店查询返回 40 条审计事件；动作 `payment` 过滤准确返回 1 条；非法日期范围被 422 拒绝。
- 审计浏览器：动作/对象/日期筛选、服务端分页、状态变化、请求号和追踪号详情完成真实页面验收；页面无写入、编辑或删除入口。
- 经营报表 API：7 日口径返回业务已结算 100 元、已记录资金 40 元、外部待核对 60 元、1 张结算单、9 次接待；支付构成、项目快照和设施会话可追溯，超过 92 天被 422 拒绝。
- 报表浏览器：每日走势、资金三口径、支付构成、服务项目排行、设施记录时长和今日经营工作台完成真实页面验收；零值日期不绘制伪柱形。
- 员工权限：新增第二名复核负责人，员工档案、登录账号、角色与门店范围原子创建；初始密码未回显，首次登录强制改密后才可进入业务页面。
- 账号生命周期：账号停用和重新启用完成真实浏览器验收，员工历史保留；当前用户自停用入口隐藏，服务端同时禁止自操作并保护最后一个有效 `OWNER`。
- 职责分离：第二名 `OWNER` 从复核队列处理原负责人班次，60 元人工微信仍保持待核对，班次从 `ReviewPending` 真实推进为 `Closed`。
- 产品目录：创建本地虚构产品 `PD001`，单位和库存预留属性正确返回；产品页面明确不产生库存余额或出入库。
- 综合定价：同一已发布价格版本真实返回 100 元服务标准价和 68 元产品标准价；展开列表按类型、单位和金额分别显示，旧价格版本保持只读。

## V1 收口结论

PRD 第一版范围中的系统基础、员工与权限、项目/产品目录和价格、设施接待、顾客与会员基础、收银结算、人工支付登记、交班和审计已经形成可运行闭环。经营工作台与报表作为额外交付也已接入真实业务数据。

真实微信/支付宝、会员资金扣减、退款、采购、产品销售、库存、工资和短信仍明确属于后续版本，不能把设计文档或人工登记解释为这些能力已经上线。生产部署前还必须完成 PostgreSQL 18.4 目标镜像复验、商户资料与密钥配置、备份恢复演练和真实门店验收。
