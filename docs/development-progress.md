# ERP 开发进度

更新日期：2026-08-20

| 模块 | 状态 | 代码 | 窄测试 | 使用说明 | 截图 |
|---|---|---|---|---|---|
| 工程与本地运行基线 | 基线完成 | .NET/React/PostgreSQL、迁移、配置与安全中间件 | 编译、依赖审计、迁移烟测通过 | [本地启动与登录](user-manual/01-local-login-and-permissions.md) | 不适用 |
| Linux发布、备份与恢复 | 生产主机蓝绿发布闭环完成 | Ubuntu初始化、SSH/UFW、不可变包、Flyway、Nginx/systemd Blue/Green、就绪门禁、定时监测、age联合备份、隔离恢复 | 仓库集成规则；linux-x64产物、清单和摘要验证；生产就绪检查 | [V2发布、备份与恢复](v2-release-and-recovery.md) | 不适用 |
| 旧系统数据迁移 | B01生产演练完成 | 验证码登录、固定源站和GET端点白名单、AES-256-GCM、断点清单、照片密文校验、幂等来源映射、异常表与非消费资金快照 | 45项迁移测试；2545条结构化记录与139张照片完整性通过；生产备份、干跑、提交、幂等复跑和跨品牌对账通过 | [迁移PRD](legacy-system-data-migration-prd.md)、[字段画像与映射](legacy-system-field-profile-and-mapping.md) | 敏感数据不归档截图 |
| 平台控制面与登录安全 | 代码闭环并发布，待运营页面验收 | 公开注册申请、独立平台账号/会话/CSRF、注册审核、原子自动编码建商户、商户启停、登录事件和平台审计 | 真实PostgreSQL注册/改密/自动编码审核/跨租户角色/停用会话/日志不可变回归；Linux schema 32就绪 | [平台管理与登录安全](user-manual/20-platform-registration-security-and-administration.md) | 待补真实页面截图 |
| 身份、组织与权限 | 本地闭环完成 | 登录/退出、数据库权限码、菜单/路由/按钮/API四层强制、兼职角色并集、员工档案、门店调动、首次改密、密码重置、账号启停 | 权限目录单元测试；真实PostgreSQL全迁移和跨模块API回归 | [角色权限闭环](modules/role-permission-enforcement.md) | 3 张，待补各岗位菜单截图 |
| 品牌与门店主数据 | 本地闭环完成 | 品牌/门店编码自动生成且不可修改、门店新增/编辑/停用/恢复、店长/员工/设施汇总、停用业务阻断、OWNER跨店范围 | 领域生命周期测试；真实PostgreSQL自动续号、篡改拒绝、跨店、最后门店保护和上下文收口回归 | [品牌与门店](user-manual/17-brand-and-store-management.md) | 待补最新页面截图 |
| 目录与价格 | 本地闭环完成 | 独立服务/产品目录、实时包含词检索、按需调价、未变价格继承、综合价格草稿编辑/取消/发布与历史快照 | 10 个目录领域测试；真实PostgreSQL草稿编辑/取消回归 | [产品目录与标准价](user-manual/10-product-catalog-and-pricing.md) | 7 张 |
| 设施接待与计时 | 本地闭环完成 | 跨店配置总览、店长展示、服务区/服务位新增编辑、可选业务说明/参考价、看板、计时与清洁 | 8 个相关领域测试；第17迁移现有库与空库烟测；浏览器编辑闭环 | [设施接待与计时](user-manual/03-facility-reception-and-timing.md) | 6 张 |
| 预约与员工排班 | 本地闭环完成 | 预约新增/修改/查询、员工与设施冲突、到店转接待、取消/爽约、员工班次新增/修改/取消 | 7个领域测试；真实PostgreSQL预约/班次/冲突/到店/终态回归；前端时间规则 | [预约与员工排班](user-manual/18-appointments-and-employee-scheduling.md) | 待补真实页面截图 |
| 顾客与会员 | 本地闭环完成 | 原名展示、手机号中段脱敏与精确查询、按需查看留痕、分级CSV导出、加密建档、卡类、开卡、三类账户 | CSV安全与权限制品测试；API/浏览器查看、导出、审计闭环 | [顾客与会员](user-manual/04-customers-and-membership.md) | 5 张 |
| 服务录单、金额确认与改价审批 | 本地闭环完成 | 待录单接待、消费单草稿、价格快照、版本化改价策略、职责分离审批、金额确认阻断、真实待办通知 | 5 个改价审批领域测试；第20迁移现有库与全迁移空库烟测；浏览器跨角色闭环 | [服务录单与金额确认](user-manual/05-service-order-and-price-confirmation.md) | 3 张 |
| 现金、人工外部收款与交班 | 本地闭环完成 | 今日收银/待处理/交班复核分区、组合分摊、现金实收与找零、首次/补打小票审计、开班/交班快照 | 10 个相关领域测试；真实PostgreSQL找零、小票序号和打印审计回归 | [支付与收银交班](user-manual/06-payments-and-cashier-shifts.md) | 7 张，待补分区与小票最新图 |
| 会员储值与资金分账 | 首个闭环完成 | 储值单、通用支付来源、本金/奖励金分账、不可变流水、交班汇总、收入隔离 | 3 个新增领域测试；迁移/数据库/浏览器闭环烟测 | [会员储值与资金分账](user-manual/11-member-topups.md) | 2 张 |
| 次卡、积分与储值部分退款 | 本地闭环完成 | 储值按累计比例退本金并收回赠送金；次卡购买/赠送余次分账、核销/撤销/过期；积分批次FIFO、增减/撤销/过期 | 第26–27版迁移；领域规则、真实HTTP/PostgreSQL及前端规则回归 | [会员扩展](user-manual/19-service-passes-points-and-partial-topup-refunds.md) | 本地真实页面已验收，待归档截图 |
| 会员余额扣款与验证码 | 本地闭环完成 | 内部账户支付、同卡先本金后奖励、完整手机号核对、500 元一次性验证码、原子扣款、不可变借方流水 | 4 个新增领域测试；迁移/数据库/浏览器闭环烟测 | [会员余额消费与手机号验证](user-manual/12-member-balance-payments.md) | 3 张 |
| 消费退款与储值退款 | 本地闭环完成 | 原分摊可退额度、部分/全额退款、储值按累计比例收回赠送金、OWNER审批、现金/会员反向流水、净额报表 | 相关领域测试；真实HTTP/PostgreSQL和浏览器闭环 | [退款与冲正](user-manual/13-payment-refunds-and-topup-reversals.md) | 2 张 |
| 微信/支付宝真实渠道 | 本地代码闭环，待真实商户验收 | 安全配置、Native/订单码、异步状态、验签、查单、关单、原路退款、日账单下载、自动匹配与差异处置 | 解析/签名/领域夹具；第14迁移现有库与空库烟测；浏览器/前端构建 | [渠道配置与对账](user-manual/14-payment-channel-configuration.md)、[退款与冲正](user-manual/13-payment-refunds-and-topup-reversals.md) | 1 张 |
| 商品销售与门店库存 | 本地闭环完成 | 整数库存余额、过账单据、不可变流水、确认预占、结算出库、作废释放、退货回库 | 7 个相关领域测试；第15迁移现有库与15迁移空库烟测；浏览器预占/释放闭环 | [商品销售与库存](user-manual/15-product-sales-and-inventory.md) | 1 张 |
| 产品图片与顾客服务档案 | 本地闭环完成 | 可选产品封面、加密图片存储、服务时间/文字/图片档案、可选订单关联、追加更正及原文历史、敏感权限和不可变约束 | 追加更正领域测试；第23迁移数据库防改触发器；真实PostgreSQL更正回归 | [产品图片与服务档案](user-manual/16-product-images-and-service-records.md) | 2 张，待补更正历史最新图 |
| 审计记录 | 首个闭环完成 | 门店范围、动作/对象/日期筛选、服务端分页、只读详情 | API 范围/过滤/非法日期烟测；浏览器列表与详情验收 | [审计记录](user-manual/07-audit-events.md) | 2 张 |
| 经营工作台与报表 | 多门店经营与对账闭环完成，视觉待验收 | 当前门店每日/期间收入、累计储值净额、7–92 日趋势；OWNER 跨店收入、储值、支付待核对、渠道差异和交班状态 | 真实 HTTP/PostgreSQL 验证两店收入归属、储值退款净额和 OWNER 汇总；前端构建通过 | [经营工作台与报表](user-manual/08-dashboard-and-operations-reports.md) | 待补白色侧栏与多门店报表截图 |

状态只以可运行代码和对应测试证据为准，设计文档或静态界面不单独视为模块完成。

## 本轮证据

- API 编译：成功，0 警告、0 错误。
- 旧系统只读导出：最终顾客快照完成23页、2287条；基础资料最终快照完成门店5、员工40、服务74、产品51、次卡8、会员卡类23、储值方案21、设施9、品牌0、单位15、员工工种4、来店渠道8，共12模块258条。13个结构化模块合计2545条；护理独立列表为0条；2287份顾客档案全部检查，导出139张AES-256-GCM加密照片，清单数、文件数和密文SHA-256逐张复算一致，2148份档案无照片。本轮只调用登录和精确登记的GET读取端点，没有调用任何旧系统新增、修改、删除、导入、退款或其他写端点。
- 旧系统字段画像：离线重新校验13个模块清单、端点、记录数和密文SHA-256后，在内存分析2545条、213个字段；输出仅含结构统计，不含来源值、值摘要、输入路径或密钥。识别出HTML字段、顾客生日混合格式、手机号重复候选、设施归属缺失以及会员资金/规则模型缺口；213个字段均进入初步映射矩阵。
- B01生产迁移：先生成schema 32加密备份，再以真实2545条来源记录和139张照片完成事务干跑；干跑后数据库和附件均恢复基线。显式提交创建5门店、40员工、74服务、51产品、23卡类、2287顾客、1874服务档案和139附件；同指纹复跑命中原运行ID且没有重复创建。2287条旧资金快照全部为不可消费，1994个无法可靠解释的生日保持为空并进入警告清单。
- 跨品牌对账：B01导入后合计6门店、41员工、24卡类；另一品牌仍为1门店、1员工、1卡类且顾客、目录、档案、附件和迁移表均为0。B01来源映射4354条，服务档案附件引用139/139完整，孤儿附件0。
- 旧系统迁移工具验证：45项测试覆盖固定HTTPS主机、登录POST例外、读取端点GET、未登记路径和写动作拒绝、多实体白名单、护理空响应、档案照片路径白名单、jqGrid分页、加密篡改拒绝、权限收紧、敏感值不进入画像、路径穿越和重复JSON字段拒绝、完整模拟登录/分页/断点恢复；完整解决方案Release构建0警告、0错误。
- 当前全量后端测试：旧系统迁移测试 45/45、领域单元测试 118/118、仓库与真实API集成测试 50/50，共213项通过且无跳过。
- 前端自动测试：25/25 通过，覆盖权限显式授予/默认拒绝、商户/平台隔离CSRF、安全请求、失效CSRF单次恢复、自动检索防抖/取消、员工详情可访问入口、现金实收提交、预约时间、储值退款分摊和次卡到期规则。
- 前端静态检查：通过，0 警告。
- 前端生产构建：通过；页面已按路由懒加载，原约1.60 MB单入口拆为独立页面块，收银页约93 kB（gzip约28 kB），最大共享块约506 kB（gzip约166 kB）。构建仅剩共享块略高于500 kB的提示，不影响产物生成。
- 依赖审计：npm 与 NuGet 当前均未发现已知漏洞。
- V2生产发布：`0.2.0-20260820.6` 包外SHA-256验证通过，Flyway从schema 31前向迁移到32并完成二次校验，Nginx蓝绿切换后就绪检查返回200。
- V2就绪门禁：最新API要求 `/health/ready` 返回200、`ready`和schema `202608200032`；服务端同时检查数据库可连接与改价、档案、库存、预约、班次、次卡、积分批次、供应商、采购、批次、盘点、调拨、平台账号、注册申请、业务编码序列、登录安全事件和旧系统迁移控制表存在。
- V2恢复演练：PostgreSQL 18.3开发库通过自定义格式流式备份恢复到全新隔离库，核心表和会员余额非负校验返回 `RESTORE_OK`；验收后隔离库删除，剩余数量为0。
- V2发布安全回归：仓库测试锁定最新迁移与就绪版本一致、包文件无多余/缺失/重复/越界、Flyway禁止baseline/clean/repair、切流失败恢复原代理、回退schema来自活动记录、备份必须age加密、恢复拒绝覆盖已有数据库。
- V2真机边界：Linux systemd、Nginx、蓝绿切流、schema门禁和age备份已在生产主机执行；外部告警接收、证书自动续期周期和持有age私钥的异机恢复仍需持续演练。
- 数据库迁移：三十二个前向迁移已由真实 PostgreSQL 集成环境从空库完整执行；第32版新增旧系统迁移运行、幂等映射、异常和非消费资金快照表及不可变约束；目标Linux只允许Flyway前向迁移。
- 生产自动编码迁移核对：历史 `B01/S01`、`B2026082001/S001` 名称、编码和启用状态保持不变；B01导入的5家旧门店自动取得 `S002` 至 `S006`，未与历史 `S01` 冲突。
- 权限闭环：有效权限由数据库 `authorization_role_permissions` 汇总，前端菜单、路由和按钮消费服务端权限码，API 命名策略逐请求再次查询授权；前台不再出现收银入口、收银员不再出现库存入口，无报表权限的工作台不请求金额接口，兼职角色按并集计算。
- 平台控制面：公开申请只创建待审核记录；平台管理员使用独立 Argon2id 账号、Cookie、CSRF 和授权策略，首次登录强制改密；批准申请在同一事务自动生成品牌编码和 `S001`，创建商户、首店、标准角色和负责人，停用商户后现有业务会话即时失效。
- 登录安全事件：商户与平台的成功、失败、锁定、退出、改密和管理员重置密码均写入只追加事件；未知账号使用 HMAC 和脱敏显示，事件不记录密码、Cookie、请求体或支付密钥，平台页面支持只读分页筛选。
- 实时索引：顾客、员工、服务项目和产品统一为300毫秒防抖自动查询并消费请求取消信号；第21迁移已启用 `pg_trgm` 并建立11个GIN模糊索引，手机号精确查询仍使用HMAC摘要。
- 高增长列表分页：顾客、员工、待录单接待、消费单、支付、退款、交班、储值、库存流水/单据、服务档案、改价审批和渠道对账统一返回`items/total/page/pageSize`，数据库稳定排序后分页；主要历史页面显示真实总数并由服务端换页，非法页码或每页超过100条返回422。
- 顾客资料生命周期：OWNER和店长编辑前必须先按需查看完整手机号并留痕；姓名、手机号、性别、生日、来源和通知授权支持版本化更新，手机号重复、并发覆盖会被拒绝；停用/恢复要求原因且保留会员余额、服务档案、订单和全部历史。
- 重复顾客归并：仅OWNER可在预览通过后确认；同店、无活动接待/未完成订单/有效验证码且无合并链冲突时，源档案写入第22版血缘。保留档案聚合源卡、余额、储值和服务档案，源手机号/姓名/卡号查询重定向到目标，历史外键和不可变流水不改写。
- 员工生命周期：OWNER可版本化编辑姓名、岗位、门店和角色，办理离职/复职、账号启停及管理员密码重置；离职同步停号、复职不自动启用，密码不回显或写入审计，当前用户和最后一个有效OWNER受双重保护。
- 品牌与门店生命周期：品牌编码由平台自动生成，门店编码按品牌 `S001`、`S002` 顺序自动分配，两类编码前后端均不可修改；OWNER编辑名称并管理门店新增、修改、停用和恢复。全部有效门店自动进入OWNER会话范围。停用前检查最后有效门店、活动设施、未完成接待/订单和未关闭班次，停用后门店上下文自动收口，历史数据不删除。
- 收银工作台与小票：页面分为今日收银、待处理和交班复核；现金支付保存实收及服务端找零。小票首次为顾客联，补打使用递增序号，每次打印均记录操作者与支付单审计。
- 响应式与版本标识：移除1180px页面硬宽度，关键列表增加显式查看按钮和焦点样式；真实浏览器在960×720、600×800均无页面级横向溢出。侧栏本地显示`0.0.0-local · 本地开发`，Linux构建强制注入SemVer与生产环境。
- 预约排班浏览器：本地真实页面在1280宽度确认“预约与排班”菜单、预约/员工排班双页签、自动查询、预约新增表单和本地版本标识；没有提交浏览器虚构业务数据，保存/冲突/终态由真实HTTP/PostgreSQL自动回归证明。
- 实时索引浏览器验收：未点击查询按钮，分别输入“演示”“系统”“owner01”“演示门店”“基础”和“PD001”，顾客、员工姓名/账号/门店、服务项目及产品列表均自动收敛到对应结果。
- 最终 API 烟测：健康检查、登录、产品、价格版本、员工、已关闭班次和经营报表均为 200；报表保持业务已结算 100 元、外部待核对 60 元。
- 首次改密后端约束：未完成改密账号调用业务接口实测返回 `403 PASSWORD_CHANGE_REQUIRED`，不能通过绕过前端路由访问业务。
- 密码策略：经产品确认，全系统最低长度调整为8位，仍必须同时包含英文字母和数字，特殊字符、大小写混合均不强制；前后端使用同一规则并由自动测试锁定。
- 浏览器：49 张本地真实页面截图覆盖登录、独立目录价格、产品图片、顾客服务档案、跨店设施配置、设施接待、顾客会员、储值、会员扣款、退款审批、支付渠道配置、商品库存、收银交班、审计、报表和员工权限；各模块手册图片存在性由集成测试检查。
- V2 渠道安全底座：真实浏览器为微信、支付宝分别保存停用配置映射，页面只回显凭据配置名和缺项；尝试启用微信时服务端以 `PAYMENT_CHANNEL_CREDENTIALS_INCOMPLETE` 拒绝，数据库未保存私钥或 APIv3 密钥字段。
- V2 渠道交易代码：微信 Native 与支付宝订单码适配器、待确认支付、付款码、主动查单、先查后关、匿名限流回调和原子结算入口已编译；没有真实商户凭据，尚未形成真实扣款证据。
- V2 渠道安全验证：微信规范化签名和 AES-GCM 通知解密、支付宝 RSA2 通知验签及篡改拒绝夹具通过；渠道金额/交易号不一致和关单后迟到支付进入失败或待冲正状态。
- V2 渠道退款代码：微信 APIv3 原路退款/查单和支付宝官方 SDK 退款/查询已接入；审批先进入 `Processing`，只有可信成功结果才在串行化事务中推进支付、消费单和退款单，固定商户退款单号支持安全重试。
- V2 渠道退款验证：签名微信退款响应夹具验证请求路径、金额和固定退款单号；第13迁移同时通过现有库升级与13个迁移空库执行；本地页面显示退款审批与渠道处理区，但没有商户凭据，因此未发起真实退款。
- V2 渠道对账代码：微信交易账单使用签名请求、受限下载域名、20 MiB 上限和渠道 SHA-1 完整性校验；支付宝使用官方 SDK 获取账单地址、受限域名、压缩包上限与 UTF-8/GB18030 解码；系统另存 SHA-256 摘要且不保存原始账单。
- V2 渠道对账验证：微信已签名账单地址响应、文件 SHA-1、支付/退款行和支付宝引号/负退款金额解析夹具通过；第14迁移同时通过现有库升级和14迁移空库执行；浏览器显示账单日、差异说明和禁用下载按钮，因没有商户凭据未发出真实账单请求。
- V2 商品库存：本地虚构商品入库10盒后，消费单确认2盒时账面10、预占2、可用8；作废后账面10、预占0、可用10，且只有入库流水、没有伪造销售出库流水。
- V2 独立定价：浏览器只勾选商品并发布新价格版本，未填写服务价格仍可保存；收银页继续显示继承的服务标准价 ¥100.00，并显示该商品新标准价 ¥26.00。验证表单均取消关闭，没有额外生成消费单。
- V2 库存数据库：第15迁移在现有开发库和15迁移全新空库均执行成功；库存流水更新/删除由数据库触发器禁止，商品退货与资金退款代码完全分离。
- V2 产品图片：浏览器给本地虚构产品上传PNG图片并在目录显示缩略图；产品价格、库存属性和历史订单未改变。
- V2 服务档案：浏览器给本地虚构顾客存档三段选填文字和一张图片，未输入或复制消费金额；页面回显服务时间、建档人、文字和受保护缩略图。
- V2 文件安全：单图5MB、JPEG/PNG/WebP真实文件头、随机存储键、加密落盘、SHA-256摘要、上传限流和角色/租户/门店/顾客读取校验已接入；第16迁移通过现有库升级与16迁移全新空库执行。
- V2 门店设施配置：独立页面按门店展示当前店长、服务区和服务位；OWNER可跨店配置，STORE_MANAGER只配置授权门店。服务/设施名称和参考单价可空，服务位名称与所属服务区保持结构必填。
- V2 设施参考价隔离：浏览器给虚构服务位保存88元参考价后，日常看板明确标注“不自动收费”；收银服务不读取设施表，消费金额仍只来自消费单价格快照和现场明确输入。
- V2 设施配置数据库：第17迁移只给现有设施表增加3个可空说明字段，现有库升级和17迁移全新空库均通过；配置编辑写入审计元数据，不覆盖历史计时记录。
- V2 改价策略：第20迁移增加租户级版本化策略、订单授权快照和独立审批表；当前空库默认店长单行优惠10%、整单优惠50元且不能直接提价，旧历史改价按兼容授权回填而不改金额。
- V2 改价服务端权限：角色由登录会话取得，客户端不能提交角色；仅OWNER可发布策略和审批，申请人自批被领域与数据库共同禁止，待审批和已驳回订单无法确认金额或收款。
- V2 改价浏览器闭环：使用临时收银员账号创建90元成交价/100元标准价草稿，页面阻止确认；OWNER从右上角真实通知进入审批，批准后才可确认。验收单随后作废，临时登录账号已停用，员工历史保留。
- V2 改价回归保护：覆盖审批阻断、申请人自批、店长阈值、优惠不能被提价抵消和作废取消审批；批准保存金额/策略/角色快照，不会重算或改写订单行。
- V2 敏感字段：顾客姓名继续按原名展示，普通接口手机号仍为前三位加四个星号加后四位；完整号码只由目的必填、限流、授权门店接口短暂返回，切换顾客即从页面清除。
- V2 顾客导出：OWNER与店长可导出原名名单，店长服务端强制脱敏、OWNER才可选择完整手机号；单次5000条，CSV防公式注入，导出文件和明文不进入审计。
- V2 敏感操作浏览器闭环：按需查看完整手机号成功并在审计页回显操作者、业务目的、请求号和追踪号；脱敏导出2条成功，审计元数据显示 `includesFullMobile=false` 和条数2。
- V2 会员储值：本地虚构会员现金储值本金 200 元、赠送奖励金 20 元，页面回显与数据库余额一致；产生 2 条不可变贷方流水。
- V2 账务隔离：数据库实测消费支付 100 元与储值收款 200 元按业务来源分开；经营报表代码只汇总消费，储值现金仍归入当前收银班次。
- V2 会员扣款：本地虚构会员先储值本金 700 元，再使用本金支付 600 元消费单；账户最终余额 100 元，支付分摊为 `InternalAccount + InternalConfirmed + NotRequired`。
- V2 扣款流水：同一会员本金账户保留 700 元贷方和 600 元借方两条不可变流水，分别关联储值单和消费单，没有覆盖原入账记录。
- V2 验证码：600 元扣款挑战绑定消费单、顾客和金额，5 分钟有效并在结算事务中推进为 `Used`；前端同时校验已验证金额没有被后续编辑改变。
- V2 扣款顺序：同卡固定先扣本金后扣奖励；领域测试覆盖本金未用完时直接扣奖励被拒，以及本金全部扣完后组合使用奖励成功。
- V2 消费退款：600 元会员本金支付真实完成 200 元部分退款，支付单、消费单均为 `PartiallyRefunded`，原本金账户新增 200 元 `PaymentRefund` 贷方流水。
- V2 储值退款：真实 PostgreSQL 验证500元本金加100元奖励退本金200元时收回40元奖励，储值/支付标记部分退款，余额为本金300元、奖励60元；原入账流水保持不变。
- V2 退款净额：经营报表按完成日展示消费退款和净收入；储值冲正不冲减消费收入，现金退款计入审批人班次现金净额。
- 设施闭环：同请求幂等重放命中同一记录；`IN_USE → PAUSED → IN_USE → 换设施 → CLEANING_REQUIRED → AVAILABLE` 真实 API 与浏览器流程通过，刷新后暂停状态保持。
- 设施并发：两个终端同时开始同一设施时实测为 `200 + 409 (VERSION_CONFLICT)`，数据库活动会话保持唯一，验收清理后为 0。
- 设施审计：开始、暂停、继续、更换、结束和清洁完成均已产生审计记录。
- 顾客会员 API：顾客建档、卡类创建和开卡同请求重放均为 200；重复手机号为 409、重复卡类编号为 422；开卡后准确产生 3 个独立零余额账户。
- 会员流水约束：真实 PostgreSQL 中更新和删除均被 `55000` 拒绝，测试事务回滚后未残留流水；从空库连续执行三个迁移得到 28 张表。
- 顾客会员浏览器：完成脱敏列表、新建顾客、卡类表单、开卡和账户详情验收；生效日按门店时区显示为 2026-08-18。
- 隐私配置：非开发环境缺少、过短或仍为占位值的手机号查询密钥时，API 已实测启动失败并给出明确配置错误。
- 消费单 API：直接补录创建与确认的同请求重放均为 `200 + 200` 且命中同一记录；消费单日期按门店时区生成。
- 消费单并发：两个终端同时为同一已结束接待录单，实测为 `200 + 409 (VERSION_CONFLICT)`，有效消费单保持唯一。
- 收银浏览器：消费单列表、新建表单、标准价/成交价差异、改价原因和待支付详情在 1280×720 视口完成真实页面验收。
- 支付结算 API：未开班使用现金/人工外部支付为 `409 SHIFT_NOT_OPEN`；开班和结算同请求重放均为 `200 + 200` 且命中同一记录。
- 组合支付：真实 API 将 100 元应收拆为 40 元现金和 60 元微信人工登记；消费单为已结算，分摊分别为 `CashRecorded + NotRequired` 与 `ManualPendingReconciliation + Pending`。
- 收银交班：理论现金 90 元、实交 90 元、差额 0、外部待核对 60 元形成不可变提交快照；重复提交命中同一班次；原收银员自我复核被 `403 FORBIDDEN_ACTION` 拒绝。
- 支付/交班浏览器：当班概览、组合支付、结算分摊、交班表单和待复核状态在 1280×720 视口完成真实页面验收。
- 审计 API：当前门店查询返回 40 条审计事件；动作 `payment` 过滤准确返回 1 条；非法日期范围被 422 拒绝。
- 审计浏览器：动作/对象/日期筛选、服务端分页、状态变化、请求号和追踪号详情完成真实页面验收；页面无写入、编辑或删除入口。
- 经营报表 API：7 日口径返回业务已结算 100 元、已记录资金 40 元、外部待核对 60 元、1 张结算单、9 次接待；支付构成、项目快照和设施会话可追溯，超过 92 天被 422 拒绝。
- 报表浏览器：每日走势、资金三口径、支付构成、服务项目排行、设施记录时长和今日经营工作台完成真实页面验收；零值日期不绘制伪柱形。
- 员工权限：新增第二名复核负责人，员工档案、登录账号、角色与门店范围原子创建；初始密码未回显，首次登录强制改密后才可进入业务页面。
- 账号生命周期：账号停用和重新启用完成真实浏览器验收，员工历史保留；当前用户自停用入口隐藏，服务端同时禁止自操作并保护最后一个有效 `OWNER`。
- 职责分离：第二名 `OWNER` 从复核队列处理原负责人班次，60 元人工微信仍保持待核对，班次从 `ReviewPending` 真实推进为 `Closed`。
- 产品目录：创建本地虚构产品 `PD001`，单位和库存预留属性正确返回；产品页面明确不产生库存余额或出入库。
- 综合定价：同一已发布价格版本真实返回 100 元服务标准价和 68 元产品标准价；展开列表按类型、单位和金额分别显示，旧价格版本保持只读。

## V1 收口结论

PRD 第一版范围中的系统基础、员工与权限、项目/产品目录和价格、设施接待、顾客与会员基础、收银结算、人工支付登记、交班和审计已经形成可运行闭环。经营工作台与报表作为额外交付也已接入真实业务数据。

真实微信/支付宝商户验收、复杂退卡、采购订单/退货/应付、工资和真实短信仍明确属于后续版本。供应商、采购入库、批次效期/成本库存、冻结账面盘点和跨店调拨已经形成本地闭环，但尚未经过真实门店实物盘点验收。生产部署前还必须完成商户资料与密钥配置、备份恢复演练和真实门店验收。
