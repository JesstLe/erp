# 实现假设与待确认项

更新日期：2026-08-18

本文件只记录开发过程中无法从现有 PRD 唯一确定、但可以通过可逆方案继续推进的事项。已确认的产品决策仍以 `product-decision-register.md` 为准。

## 当前采用的可逆假设

| 编号 | 状态 | 当前实现 | 影响与后续调整方式 |
|---|---|---|---|
| IMP-001 | 待确认 | 部署目标和 `compose.yaml` 固定 PostgreSQL 18.4、端口默认 54318；因镜像拉取受阻，本轮迁移烟测临时使用本机已有 PostgreSQL 18.3 镜像并已通过 | 18.3 结果只证明当前 SQL 在同一主版本可执行；正式基线仍须在 18.4 镜像拉取成功后复验 |
| IMP-002 | 待确认 | V1 管理端界面默认使用简体中文、人民币和 Asia/Shanghai 展示时区 | 时区按门店字段预留；国际化后续可增加资源文件 |
| IMP-003 | 待确认 | 首个界面优先适配 1440×900 的门店电脑，同时保证 1280px 宽度可用 | 后续拿到实际收银设备分辨率后调整断点 |
| IMP-004 | 待确认 | 真实微信/支付宝功能默认关闭，先实现人工外部收款待核对 | 已由PRD允许；商户模式确认后启用渠道适配器 |
| IMP-005 | 待确认 | 报表第一批按营业额、客流、设施利用率、支付构成和待核对金额展示 | 指标口径会在报表模块文档中逐项标记并可版本化调整 |
| IMP-006 | 待确认 | 仅在ASP.NET Core `Development` 环境允许回环地址使用非Secure开发Cookie，便于本机浏览器联调 | 非Development环境始终强制Secure Cookie、HTTPS和HSTS；不影响正式安全基线 |

## 处理规则

- 待确认项不得写成不可逆数据库约束或散落的代码常量。
- 可配置项通过设置、功能开关或版本化策略实现。
- 用户确认后，在本文件保留历史并把状态改为“已确认”或“已替代”。
