# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)；在形成首个可发布版本前，变更记录在 `Unreleased`。

## [Unreleased]

### Added

- 启动 V2，实现会员储值单、储值查询、本金/奖励金分账和不可变入账流水。
- 将支付单兼容扩展为通用业务来源，旧消费支付自动回填；经营收入排除储值，交班继续汇总实际收款。
- 增加会员储值组合收款、开班约束、人工外部待对账、幂等、并发保护和储值审计。
- 增加会员储值表单、账户余额与历史回显，并形成两张本地虚构数据截图手册。
- 建立 V2 完成定义与七条端到端交付链，明确下一步为会员余额消费与验证码。

- 建立 .NET 10、React 19、PostgreSQL 18.4 的单仓库工程基线。
- 建立独立 Git 仓库、密钥忽略规则、本地数据库编排和窄测试优先策略。
- 纳入 PRD、状态机、权限阈值、数据库治理与支付开发文档作为实现依据。
- 实现租户、门店、员工身份、角色动作授权和开发环境初始化数据。
- 实现服务项目、价格草稿、价格版本发布及已发布版本只读快照。
- 实现 React 登录页、经营工作台、门店上下文、服务项目页和价格版本页。
- 为登录与目录价格模块建立带真实界面截图的用户手册。
- 实现可配置设施分组、设施类型与设施档案，以及设施看板。
- 实现服务器计时的开始、暂停、继续、原子换设施、结束和清洁完成闭环。
- 设施操作使用全局幂等请求号、数据库并发唯一约束和逐动作审计；设施时长不自动形成收费。
- 为设施接待模块建立字段规则、操作边界和真实页面截图手册。
- 实现顾客脱敏查询、加密手机号建档、重复候选阻断和门店数据范围校验。
- 实现会员卡类、会员开卡以及本金、奖励金、积分三个独立零余额账户的原子初始化。
- 增加不可更新、不可删除的会员账户流水表，为后续储值、扣费与冲正提供账务基线。
- 为顾客与会员模块建立字段规则、隐私边界和真实页面截图手册。
- 实现服务结束后直接补录或从已结束接待创建消费单草稿，并支持可选关联顾客。
- 实现服务项目名称、编号、标准价和成交价的不可追改价格快照；项目实际时长与设施时长均不参与自动计费。
- 实现标准价偏离时的强制改价原因、消费单金额确认、待支付状态、幂等重放、并发唯一约束和审计。
- 为服务录单与金额确认模块建立字段规则、状态边界和真实页面截图手册。
- 实现可配置支付方式、支付单和支付分摊，支付合计必须等于服务端锁定的消费单应收金额。
- 实现现金记录和微信/支付宝人工登记；人工外部收款始终保持待核对，不伪装为渠道确认。
- 实现现金与人工外部支付先开班、消费单原子结算、接待完成、幂等重放和审计闭环。
- 实现收银班次的备用金、理论现金、实交现金、现金差额、外部待核对快照、提交交班和独立复核规则。
- 为支付与收银交班模块建立字段规则、职责分离、对账状态和真实页面截图手册。
- 实现按授权门店、动作、对象和日期筛选的只读审计中心，支持服务端分页和操作者名称解析。
- 实现审计详情中的状态变化、业务原因、幂等请求号和服务追踪号展示，并明确其不等同于支付凭证。
- 为审计模块建立查询规则、权限边界、字段解释和真实页面截图手册。
- 实现按门店时区和日期范围聚合的经营报表，单次查询限制为 92 天。
- 分开展示业务已结算、已记录资金和人工外部待核对，并提供每日走势和支付方式构成。
- 实现基于历史成交快照的服务项目排行，以及扣除暂停并截断日期边界的设施记录时长汇总。
- 将经营工作台从静态占位改为真实今日接待、设施状态、已记录资金和待核对金额。
- 为经营工作台与报表模块建立指标口径、权限边界、待确认项和真实页面截图手册。
- 实现员工档案与登录账号分离、员工门店任职、登录门店范围和多角色分配。
- 新账号首次登录强制修改初始密码；初始密码不回显，账号停用保留员工与历史业务。
- 增加账号自停用和最后一个有效 `OWNER` 保护，并为员工创建和账号状态变化记录审计。
- 在收银页增加交班复核队列，由另一名授权人员完成关班；外部人工收款继续保持待核对。
- 为员工账号、首次改密和独立交班复核建立真实页面截图手册。
- 实现产品基础目录，保存产品编码、名称、计量单位和后续库存属性预留标记。
- 扩展价格版本，使同一版本分别保存服务项目价格和产品价格，发布后保持只读历史快照。
- 增加产品目录与综合定价页面，并明确第一版不产生产品销售或库存流水。
- 为产品建档、综合定价和已发布价格建立真实页面截图手册。
- 合并形成单一《门店 ERP V1 用户使用手册》，串联空库初始化和完整门店业务闭环。
- 增加迁移非破坏性与合并手册截图存在性的仓库集成测试。

### Changed

- 本地 Development 环境使用独立的非 `__Host-` Cookie 名称，生产环境继续强制 `__Host-`、Secure Cookie、HTTPS 和 HSTS。
- 账户区提供显式“退出登录”按钮，便于公用门店终端安全交接。
- 退出登录时取消在途用户查询并移除身份缓存，避免已退出会话被旧缓存重新带回工作台。
- 消费单号和直接补录接待号改为按门店时区生成日期，避免跨 UTC 日期边界出现门店日期错误。
- 价格版本加载改用拆分查询，避免服务项目与产品价格集合产生笛卡尔放大。

### Security

- 赠送奖励金只能由 `OWNER` 确认；店长和收银员只能办理无赠送储值，服务端不信任前端禁用状态。
- 会员账户不能用于购买自身储值；资金金额使用整数分，写入使用串行化事务、幂等请求号和账户版本并发控制。

- 本地数据库只绑定回环地址，容器启用 `no-new-privileges`。
- 环境变量模板只提供占位符，不提交数据库密码或初始账号密码。
- 密码采用 Argon2id 哈希；登录接口按来源限流；所有写接口验证防跨站请求令牌。
- 首次登录修改密码同时由前端路由和服务端中间件强制执行，未改密账号不能调用业务接口。
- 员工自停用和最后一个有效最高权限账号由服务端阻止；账号状态变化保留审计事件。
- 默认响应启用 CSP、禁止 iframe 嵌入、禁用 MIME 嗅探并收紧浏览器能力策略。
